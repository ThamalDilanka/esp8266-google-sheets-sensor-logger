#pragma once

// Copy to logger_secrets.h. Keep the real values local and out of Git.
// Use the production Apps Script URL ending in /exec.
constexpr char LOGGER_SCRIPT_URL[] = "";
// Exactly 32 hexadecimal characters; match DEVICE_TOKEN in Script Properties.
constexpr char LOGGER_DEVICE_TOKEN[] = "";
