#pragma once

// Copy to wifi_secrets.h and fill in your network credentials locally.
// An empty SSID runs only the network scan. The password is never printed.
constexpr char WIFI_SSID[] = "";
constexpr char WIFI_PASSWORD[] = "";
